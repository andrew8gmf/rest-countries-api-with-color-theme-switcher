import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'rest-countries-api-with-color-theme-switcher',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
